#!/bin/bash
set -e

echo "Uninstalling codebuff..."

# Check if codebuff is installed before trying to uninstall
if npm list -g codebuff >/dev/null 2>&1; then
    npm uninstall -g codebuff
    echo "codebuff uninstalled."
else
    echo "codebuff was not installed via npm."
fi

exit 0
